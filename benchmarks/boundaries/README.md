# Boundaries

This folder contains mined decision-boundary outputs.

## Purpose

These reports convert per-scenario winner analysis into provisional routing rules.

## Why this matters

This helps turn benchmark results into explicit policy guidance such as:
- when compression is enough
- when phrase-saver escalation is preferred
- when hybrid fallback is likely necessary
